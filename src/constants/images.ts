// Recorte das pessoas do mockup do hero (fundo transparente, base em degradê). ~100 KB.
export const heroPeopleImage = require('../../assets/images/hero/hero-people.webp');

export const heroPeopleRatio = 900 / 664;

// Foto do login, recortada do mockup image/login-modelo.png (texto da tela removido).
// As bordas esquerda e de cima já vêm transparentes, então ela se funde ao fundo de qualquer tema. ~60 KB.
export const loginPeopleImage = require('../../assets/images/auth/login-people.webp');

export const loginPeopleRatio = 581 / 884;
